server:
  port: 8082

spring:
  application:
    name: jwt-auth

app:
  jwt:
    # Must be a Base64-encoded key >= 256 bits for HS256. Generate your own
    # for real use, e.g.: openssl rand -base64 32
    expiration-ms: 3600000
